#!/bin/bash
#
# <> aliases
#
alias kubectl="microk8s kubectl "
alias k8ctl="microk8s kubectl "
alias mk8status="microk8s status --wait-ready "
alias k8getpods="microk8s kubectl get pods "
alias k8applfile="microk8s kubectl apply -f "
alias k8delfile="microk8s kubectl delete -f "
